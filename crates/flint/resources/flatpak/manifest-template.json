{
  "id": "$APP_ID",
  "runtime": "org.freedesktop.Platform",
  "runtime-version": "23.08",
  "sdk": "org.freedesktop.Sdk",
  "sdk-extensions": [
    "org.freedesktop.Sdk.Extension.rust-stable"
  ],
  "command": "flint",
  "finish-args": [
    "--talk-name=org.freedesktop.Flatpak",
    "--device=dri",
    "--share=ipc",
    "--share=network",
    "--socket=wayland",
    "--socket=fallback-x11",
    "--socket=pulseaudio",
    "--filesystem=host"
  ],
  "build-options": {
    "append-path": "/usr/lib/sdk/rust-stable/bin"
  },
  "modules": [
    {
      "name": "flint",
      "buildsystem": "simple",
      "build-options": {
        "env": {
          "APP_ID": "$APP_ID",
          "APP_ICON": "$APP_ID",
          "APP_NAME": "$APP_NAME",
          "BRANDING_LIGHT": "$BRANDING_LIGHT",
          "BRANDING_DARK": "$BRANDING_DARK",
          "APP_CLI": "flint",
          "APP_ARGS": "--foreground %U",
          "DO_STARTUP_NOTIFY": "false"
        }
      },
      "build-commands": [
        "install -Dm644 $ICON_FILE.png /app/share/icons/hicolor/512x512/apps/$APP_ID.png",
        "envsubst < flint.desktop.in > flint.desktop && install -Dm755 flint.desktop /app/share/applications/$APP_ID.desktop",
        "envsubst < flatpak/flint.metainfo.xml.in > flint.metainfo.xml && install -Dm644 flint.metainfo.xml /app/share/metainfo/$APP_ID.metainfo.xml",
        "sed -i -e '/@release_info@/{r flatpak/release-info/$CHANNEL' -e 'd}' /app/share/metainfo/$APP_ID.metainfo.xml",
        "install -Dm755 bin/flint /app/bin/flint",
        "install -Dm755 libexec/flint-editor /app/libexec/flint-editor",
        "install -Dm755 lib/* -t /app/lib"
      ],
      "sources": [
        {
          "type": "archive",
          "path": "./target/release/$ARCHIVE"
        },
        {
          "type": "dir",
          "path": "./crates/flint/resources"
        }
      ]
    }
  ]
}
