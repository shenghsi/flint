name: deploy_nightly_docs
on:
  push:
    branches:
      - main
jobs:
  deploy_docs:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Docs deployment skipped — no docs infrastructure configured for this fork"
