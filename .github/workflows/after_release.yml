name: after_release
on:
  release:
    types:
      - published
  workflow_dispatch:
    inputs:
      tag_name:
        description: tag_name
        required: true
        type: string
      prerelease:
        description: prerelease
        required: true
        type: boolean
      body:
        description: body
        type: string
        default: ""
jobs:
  stub:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "After-release hooks skipped — no external infrastructure configured for this fork"
